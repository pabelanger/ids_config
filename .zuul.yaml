---
- job:
    name: ansible-tox-molecule
    parent: tox
    vars:
      tox_envlist: molecule

- project:
    check:
      jobs:
        - ansible-tox-linters
        - ansible-tox-molecule
    gate:
      jobs:
        - ansible-tox-linters
        - ansible-tox-molecule
